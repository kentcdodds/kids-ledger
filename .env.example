# Example environment variables for local dev and preview deploys.
# Copy this to `.env` for local development, or let CI use it for previews.

# Required
COOKIE_SECRET=PREVIEW_ONLY_COOKIE_SECRET_32_CHARS_MINIMUM____

# Optional (preview uses request origin by default)
APP_BASE_URL=

# Resend (optional; preview deploys overwrite these to point at the mock services)
RESEND_API_BASE_URL=
RESEND_API_KEY=
RESEND_FROM_EMAIL=reset@kids-ledger.dev

